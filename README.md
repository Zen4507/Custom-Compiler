# Custom-Compiler
A custom statically typed compiler built with a Python frontend and LLVM backend. Features a multi-phase compilation pipeline, strict semantic validation, and JIT-based native machine code execution.
